const state = {
	isLoading: false,
	errors: null,
};

const getters = {};

const mutations = {
	loadDataStart() {},

	loadDataSuccess() {},

	loadDataFailure() {},
};

const actions = {
	loadData() {},
};

export default {
	state,
	getters,
	mutations,
	actions,
	namespaced: true,
};
